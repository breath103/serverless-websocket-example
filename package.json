{
  "name": "lambda-microservice-template",
  "version": "0.0.1",
  "description": "Template for microservice using lambda / typescript / serverless",
  "main": "index.js",
  "scripts": {
    "build": "rm -Rf dst && tsc && cp package.json dst/package.json && cd dst && npm install --cache=./.npm --production && zip -r ../dst.zip . ** && cd ..",
    "pretest": "tsc -p ./tsconfig.test.json",
    "test": "mocha -t 20000 dst/__test__",
    "deploy" : "npm run build && serverless deploy"
  },
  "author": "Kurt Lee",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^1.0.3",
    "typescript": "^2.0.6"
  }
}
